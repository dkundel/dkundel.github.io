include mixins/resume-mixins.jade

h1#resume Resume

:pfm
  This is just an excerpt of my resume. For the whole resume please follow the link in the bottom of this section.
  It will allow you to download you a full version as a PDF.

#resumePaper
  .row
    .col-xs-10.col-xs-offset-1
      .row
        .col-xs-12
          h3 education
          hr
          - var education = resume.education.filter((val) => !val.hidden) 
          each val in education
            +educationEntry(val)
              
      .row
        .col-xs-12
          h3 work experience
          hr
          - var work = resume.work.filter((val) => !val.hidden)
          each val in work
            +workEntry(val)
            
      .row
        .col-xs-12
          h3 volunteering
          hr
          - var volunteer = resume.volunteer.filter((val) => !val.hidden)
          each val in volunteer
            +volunteerEntry(val)
            
      .row
        .col-xs-12.col-sm-6
          h3 skills
          hr
          .row
            each val in resume.skills
              +skillEntry(val)
        
        .col-xs-12.col-sm-6
          h3 languages
          hr
          each val in resume.languages
            +languageEntry(val)
        
            
                
              
.row
  .col-xs-12.col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2.col-lg-6.col-lg-offset-3 
    p     
      a#resumeDownloadButton.btn.btn-primary.btn-block(href='/dominik-kundel.pdf', target='_blank') Download Resume